import { useDataStore } from './data-store'

export default definePreloadMiddleware(async (_to, _from) => {
  const dataStore = useDataStore()
  // ==============================================================
  // 権限によるガード
  // ==============================================================

  // ==============================================================
  // 不正な直アクセスのガード（ステートマシン）
  // ==============================================================

  // ==============================================================
  // 機能共通データのロード
  // ==============================================================
  await dataStore.initialize()
})
